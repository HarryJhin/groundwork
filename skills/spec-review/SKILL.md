---
name: spec-review
description: 스펙 문서를 리뷰어 패널로 검증한다. finding-unknowns가 스펙 작성 직후 호출한다. Use when 스펙 리뷰, 스펙 검증, 리뷰 패널, spec review.
---

# spec-review

스펙을 축별 리뷰어에게 병렬로 던지고 돌아온 발견을 저자가 처리해 문서를 고친다. 진행을 막는 발견이 남지 않을 때까지 돌고 라운드 상한은 3이다.

리뷰 상태를 파일로 남기지 않는다. 리뷰어의 반환 텍스트가 발견의 전부이고 처리 결과는 스펙 본문의 개정으로 드러난다. 리뷰 라운드나 발견을 추적하는 별도 파일, 상태 값, 식별자를 만들지 않는다.

플랜 리뷰는 이 스킬이 맡지 않는다. `groundwork:plan-review`가 자기 로스터와 프롬프트로 따로 돈다.

## 입력·주체·출력

리뷰 대상 스펙의 경로를 호출자에게 받는다. `groundwork:finding-unknowns`가 스펙 작성 직후 호출하면서 그 경로를 넘긴다. 사용자가 직접 불러 경로가 없으면 어느 스펙을 리뷰할지 묻는다. 임의로 찾아 고르지 않는다.

이 스킬을 로드한 에이전트가 `저자`이자 `메인`이다. 스펙 본문을 고치는 것도, 리뷰어를 띄우고 반환을 모으는 것도 그 에이전트다. 서브에이전트로 띄우는 것은 `리뷰어`뿐이다.

출력은 3가지 상태 중 하나다. `통과`면 호출한 스킬로 복귀한다. 호출자 없이 직접 실행됐으면 통과 사실과 개정 요지를 사용자에게 보고하고 끝낸다. `조건부 통과`면 남은 발견과 그 사유를 사용자에게 보고하고 끝낸다. 「라운드 계속 결정」에서 이미 물었으므로 종료 시점에 다시 묻지 않는다. `건너뜀`이면 사용자가 리뷰를 돌리지 않기로 했다는 사실을 반환한다. 어느 상태로 끝났는지를 호출자와 사용자 양쪽에 밝힌다.

## blocking 판정

리뷰어는 발견에 심각도를 매기지 않는다. 관찰과 결과만 내고 그 발견이 진행을 막는지는 저자가 발견을 처리하면서 정한다.

판정 권한이 저자에게 있는 이유는 정보의 위치다. 리뷰어는 자기 렌즈 안에서만 문서를 보므로 그 발견이 문서 전체에서 무엇을 뜻하는지 모른다. 같은 문장이 한 렌즈에는 치명적이고 다른 렌즈에는 사소해 보이는 일이 정상이다. 문서 수준의 무게는 개정을 해 본 저자에게만 보인다.

- **blocking**: 그대로 두면 이 문서로 작업하는 사람이 틀린 것을 만든다.
- **non-blocking**: 그 사람이 되물어 해소하거나 다음 단계 문서가 정할 세부다.

저자의 판정도 검사 대상이다. `non-blocking`으로 넘긴 발견은 그 판정과 근거를 다음 라운드 재리뷰에 실어 리뷰어가 재판정하게 한다. 저자는 루프를 끝내고 싶은 쪽이라 자기 판정을 혼자 확정하지 않는다.

## 리뷰 범위 결정 (사용자 게이트)

리뷰는 사용자가 치르는 비용이다. 로스터를 정하기 전에 AskUserQuestion으로 범위를 묻는다. 저자가 임의로 정하지 않는다.

문항은 하나이고 선택지는 아래 4가지를 그대로 쓴다. 순서를 바꾸거나 항목을 빼거나 새로 만들지 않는다.

| 선택지 | 뜻 |
|---|---|
| 전체 (추천) | 필수 리뷰어 전원에 조건을 만족한 재량 리뷰어를 더한다 |
| 필수만 | 필수 리뷰어 전원. 재량 축은 건너뛴다 |
| 지정 | 사용자가 렌즈를 직접 고른다. 아래 로스터를 이름과 판정 축까지 제시하고 다시 묻는다 |
| 건너뜀 | 리뷰를 돌리지 않는다 |

각 선택지 설명에 그 범위의 리뷰어 수를 적는다. 비용을 보고 정하게 하는 것이 이 문항의 목적이라 수가 빠지면 문항이 성립하지 않는다.

`추천`은 항상 `전체`에 붙인다. 문서가 작아 보인다거나 개정 범위가 좁다는 저자의 판단으로 추천을 옮기지 않는다. 저자는 리뷰를 줄이고 싶은 쪽이라 그 판단을 신뢰하지 않는다.

`건너뜀`을 고르면 리뷰 없이 끝났다는 사실을 호출자에게 반환하고 사용자 보고에도 남긴다. 그 스펙은 통과 스펙이 아니므로 통과라고 부르지 않는다.

## 리뷰어 로스터

### 필수

문서 내용과 무관하게 항상 디스패치한다.

- `junior-read`: 자기완결 판독성. 축은 `지식경계`·`참조해소`·`전방참조`·`서술판독`·`단독판독`·`표기체계`·`제작사정`·`구조중복` 8가지이고 정의는 `groundwork:writing-for-junior`에 있다
- `completeness`: 누락·엣지케이스·결정 이월. 여기에 조사·인터뷰 게이트(근거 없는 단정, 검사되지 않은 전제, 사용자 확정 없는 제외 범위·완료 판정, 포기한 대안의 부재)를 항상 함께 본다
- `consistency`: 내부 모순·절 간 충돌
- `clarity`: 모호어·이중 해석·불명확한 요구
- `scope`: 범위 이탈·독립 서브시스템 혼재. 판정 기준이 "단일 구현 플랜 하나에 담기는가"라 스펙을 쪼갤지 정하는 축이다
- `yagni`: 불요 기능·과설계·조기 추상화

### 재량

문서 내용이 조건을 만족하면 추가한다.

- `experience`: 사용자 대면 요소(UX·카피·프롬프트·에러 메시지)가 있으면
- `facts`: 외부 사실 주장(수치·인용·API 시그니처 등 문서 밖 근거에 기대는 주장)이 있으면
- `crossref`: 연관 산출물(다른 리포·문서·하위 산출물과의 정합)이 있으면
- `intent`: 문서의 결정·범위·non-goal을 대조할 인터뷰 답변·승인 대화 맥락이 있으면

선정 근거는 대화 맥락에만 둔다. 로스터 선언을 파일로 남기지 않는다.

## 리뷰어 디스패치 방식

리뷰어는 정의 파일(에이전트 타입)이 아니라 프롬프트 템플릿 주입으로 돈다.

- 대상 서브에이전트 타입은 항상 `general-purpose`다.
- 경로 기준은 플러그인 루트다. 아래 경로의 `${CLAUDE_PLUGIN_ROOT}`는 groundwork 플러그인이 설치된 디렉터리이고 실행 시점 작업 디렉터리가 아니다.
- `junior-read`의 지시는 `${CLAUDE_PLUGIN_ROOT}/skills/writing-for-junior/junior-read-prompt.md`를 Read로 읽어 주입한다. 이 렌즈는 문서 유형과 무관한 공통 렌즈라 `groundwork:writing-for-junior` 스킬(주니어 독자 기준 작성 규범과 판정 렌즈의 정본)이 소유한다. 스펙 리뷰에는 델타를 동봉하지 않는다.
- 나머지 리뷰어의 지시는 `${CLAUDE_PLUGIN_ROOT}/skills/spec-review/<lens>-prompt.md`를 Read로 읽어 그 전문을 서브에이전트 프롬프트 본문으로 주입한다. `<lens>`는 위 로스터의 리뷰어 이름을 그대로 쓴다.
- 도구 격리는 프롬프트 지시로 대체한다(각 프롬프트가 자기 도구 범위를 명시한다). 별도 도구 화이트리스트 파라미터는 쓰지 않는다.
- **리뷰어마다 모델을 명시해 띄운다.** 렌즈별 티어는 `${CLAUDE_PLUGIN_ROOT}/skills/subagent-driven-development/choosing-model-tier.md`의 「문서 리뷰 렌즈」 표에 있다. 모델을 빠뜨리면 세션 모델을 상속해 로스터 전원이 최상위 모델로 돈다.
- 한 메시지에서 병렬로, 반드시 동기(`run_in_background: false`)로 띄운다. 메인이 각 리뷰어의 `REVIEW_VERDICT` 최종 줄을 그 자리에서 모아 종료를 판정해야 한다.

동봉물은 리뷰어마다 갈린다.

- 전원: 스펙 경로.
- `junior-read`: 경로만. 세션 요약·작업 배경을 동봉하지 않는다(격리 자체가 판정 도구다).
- `intent`: 사용자 요청 원문, 인터뷰 Q&A, 승인 발화를 verbatim 동봉한다. 어느 발화를 실을지는 저자가 고르되 고른 것을 요약하거나 다듬지 않는다. 저자의 요약이 섞이면 `intent`가 대조할 원본이 저자 해석으로 오염돼 이 렌즈가 성립하지 않는다. 스펙은 인터뷰 내용을 담는 절을 두지 않으므로(스펙 절 구성의 정본은 `groundwork:finding-unknowns`의 「스펙에 담는 것」이다) 문서 안에 대체 원본이 없다. 원문이 세션 휘발로 부재하면 사용자에게 요청하고 받지 못하면 `intent`를 로스터에서 뺀다.
- `experience`·`crossref`: 각 프롬프트가 요구하는 참조물 목록.

## 라운드 계속 결정 (사용자 게이트)

3라운드를 돌고도 `blocking` 잔여가 있으면 AskUserQuestion으로 묻는다. 저자가 혼자 끝내거나 혼자 더 돌리지 않는다.

문항에 남은 `blocking` 발견의 수와 각 요지를 함께 낸다. 선택지는 아래 3가지를 그대로 쓴다.

| 선택지 | 뜻 |
|---|---|
| 확인 라운드 (추천) | 마지막 개정이 건드린 절을 담당하는 렌즈만 다시 돌린다 |
| 전원 한 라운드 더 | 로스터 전원을 다시 돌린다 |
| 여기서 종료 | `조건부 통과`로 끝낸다 |

`확인 라운드`든 `전원 한 라운드 더`든 그 한 라운드로 끝이다. 그 뒤에도 잔여가 있으면 다시 묻지 않고 `조건부 통과`로 끝낸다. 같은 문항을 반복 발행하는 것은 사용자에게 판단을 넘기는 것이 아니라 지치게 하는 것이다.

## Workflow

1. **범위 결정**: 위 「리뷰 범위 결정」 문항을 발행한다. `건너뜀`이면 그 사실을 반환하고 여기서 끝낸다.
2. **로스터 선정**: 결정된 범위로 로스터를 확정한다. `전체`면 필수 리뷰어 전원에 조건을 만족한 재량 리뷰어를 더한다.
3. **디스패치**: 로스터 전원을 한 메시지에서 병렬 동기로 띄운다.
4. **발견 처리**: 전 리뷰어의 반환을 모아 한 번에 처리한다. 도착하는 대로 하나씩 고치지 않는다.
   - **수용**: 스펙 본문을 수용한 결론으로 다시 쓴다. 대응 흔적("리뷰 반영", "`consistency` 지적에 따라")을 본문에 남기지 않는다. 개정 조항을 참조하는 다른 절을 grep으로 찾아 함께 고친다.
   - **불수용**: 근거를 정리해 둔다. 다음 라운드 재리뷰 프롬프트에 그대로 실어 리뷰어가 재판정하게 한다.
   - **사용자행**: `해소하는 데 필요한 것` 슬롯이 `사용자만 안다`인 발견은 저자가 정하지 않는다. AskUserQuestion으로 묻고 답을 본문에 반영한다. 발견 하나당 한 문항이고 한 라운드에서 나온 문항은 같은 시점에 모아 발행한다.
   - **분류**: 처리한 발견마다 `blocking`인지 정한다(위 「blocking 판정」). `non-blocking`은 판정과 그 근거를 남겨 다음 라운드에 싣는다.
5. **재리뷰**: FAIL을 낸 리뷰어에게만 재디스패치한다. 개정된 스펙 경로와 함께 이전 라운드의 자기 발견, 그에 대한 저자 처리(수용 시 개정 내용, 불수용 시 근거, `non-blocking` 판정 시 그 근거)를 동봉한다. 조기 PASS 리뷰어는 다시 부르지 않는다. 새 발견이 나왔고 라운드가 남았으면 4로 돌아간다.
6. **종료**: `blocking` 잔여가 없으면 `통과`로 호출 스킬에 복귀한다. 라운드 상한에 닿았는데 잔여가 있으면 위 「라운드 계속 결정」 문항을 발행하고 그 결과로도 잔여가 남으면 `조건부 통과`로 끝낸다. 남은 발견은 스펙 밖에서 사용자에게 그대로 보고한다. 잔여를 스펙 본문에 적지 않는다(그것은 제작 과정이다).

## Rules

- **라운드 상한은 3이다.** 상한에 닿으면 저자가 계속 여부를 정하지 않고 위 「라운드 계속 결정」 문항으로 사용자에게 넘긴다. 상한이 있는 이유는 개정이 새 발견을 낳기 때문이다. 저자가 문서를 고치는 한 리뷰어는 볼 것이 계속 생기므로 전원 동시 만족은 수렴 조건이 아니다.
- 확인 라운드의 대상 렌즈는 저자가 고른 절 목록으로 정해진다. 저자가 범위를 좁게 잡으면 확인이 형해화되므로 어느 절을 고쳤는지를 그 프롬프트에 함께 실어 리뷰어가 범위 자체를 검증하게 한다.
- 사용자 게이트 문항의 선택지는 이 문서가 정한 것만 쓴다. 상황에 맞춰 새 선택지를 만들거나 추천을 옮기지 않는다. 저자가 선택지를 지어내면 사용자는 저자가 원하는 답을 고르게 된다.
- 같은 발견이 두 라운드 연속으로 재제기되면 저자와 리뷰어가 합의하지 못한 것이다. **그 발견만** 그 자리에서 AskUserQuestion으로 넘기고 답을 본문에 반영한다. 이것은 발견 하나의 처리이지 라운드 종료가 아니다. 라운드 수는 위 상한 규칙만 정한다.
- 동일 클래스 결함이 반복 제기되면 개별 수정을 멈추고 클래스를 없애는 구조 변경을 택한다.
- 불수용은 근거와 함께 재리뷰에 부친다. 처리 없이 넘기는 것만 금지한다. 리뷰어가 근거를 받고도 같은 발견을 내면 위 두 라운드 규칙으로 간다.
- 개정으로 재량 리뷰어 조건이 새로 성립하면 로스터에 추가하고 새로 추가한 리뷰어에게 개정된 스펙 전체를 대상으로 한 번 디스패치한다.
- 종료 후 스펙을 고치면 그 절을 담당하는 렌즈에 재리뷰를 돌린다. 전원을 다시 부르지 않는다.
- 리뷰어 호출 실패(타임아웃·오류로 반환을 받지 못한 경우)는 한 번 재시도하고 재실패하면 그 리뷰어를 미통과로 보고한다. `REVIEW_VERDICT: FAIL` 반환은 실패가 아니라 정상 판정이므로 위 라운드 규칙으로 처리한다.
